import { RedApplyDirective } from './red-apply.directive';

describe('RedApplyDirective', () => {
  it('should create an instance', () => {
    const directive = new RedApplyDirective();
    expect(directive).toBeTruthy();
  });
});
